#include <stdio.h>

int main() {
    int num, sum = 0, rem;

    printf("Enter a number: ");
    scanf("%d", &num);

    for (; num != 0; num = num / 10) {
        rem = num % 10;
        sum = sum + rem;
    }

    printf("The sum of digits of the given number is: %d", sum);

    return 0;
}
